import { BookOpen, PenTool } from 'lucide-react';
import type { Algorithm } from '@shared/schema';

interface HandwrittenNotesProps {
  algorithm: Algorithm;
}

export function HandwrittenNotes({ algorithm }: HandwrittenNotesProps) {
  // Component removed - no handwritten content displayed
  return null;
}